name: taxi/mixed-sources
version: 0.3.0
sourceRoot: src/
additionalSources: {
   ## Note that using a wildcard here is incorrect, as it will compile both, which leads
   ## to duplicate definitions.
   ## We may need to revisit that later.
   "@orbital/xsd" = "xsd/FSAHSFFeedHP-v2-0.xsd"
}
