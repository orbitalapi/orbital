git {
   diskChangeDetectionMethod = POLL
   diskPollFrequency = PT120S
   repositories = [
      {
         branch = main
         name = test-git-project
         uri = "https://gitlab.com/vyne/test-project.git"
      }
   ]
}
