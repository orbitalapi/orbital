spring:
   kafka:
      consumer:
         bootstrap-servers: localhost:9092
         group-id: "cask"
         auto-offset-reset: earliest

cask:
   kafka:
      enabled: true
      topic: fpml
      consumer-count: 6
      type-name: loadtest.Swap
      # json or xml
      ingestion-type: xml


