spring:
   application:
      name: pipeline-runner

server:
   port: 9610

service:
   cask:
      name: cask

feign:
   client:
      config:
         default:
            connectTimeout: 2000
            readTimeout: 2000
