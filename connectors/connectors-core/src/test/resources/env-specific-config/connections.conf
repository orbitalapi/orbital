kafka {
   ## This should be overridden by the entry in connections.preprod.conf
   my-kafka-connection {
      connectionName=my-kafka-connection
      connectionParameters {
         brokerAddress=${NEBULA_KAFKA_BOOTSTRAP_SERVERS}
         groupId=orbital
      }
   }
}
