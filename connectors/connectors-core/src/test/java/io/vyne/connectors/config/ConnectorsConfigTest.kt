package io.vyne.connectors.config

import org.junit.Test
import org.junit.jupiter.api.Assertions.*

class ConnectorsConfigTest {

   // We write to CBOR when sending config to a remote query execution node / lambda
   @Test
   fun `can serde connectors config to CBOR`() {

   }

   @Test
   fun `can read from disk`() {

   }
}
