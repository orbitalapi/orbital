<?xml version="1.0" encoding="UTF-8"?>
<configuration>

   <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
         <!-- Don't use %L or %M for Production or Perf test -->
         <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %logger - %msg%n</pattern>
      </encoder>
   </appender>

   <logger level="INFO" name="org.hibernate"/>
   <logger level="DEBUG" name="io.vyne"/>
   <!--   For graph viz, uncomment the following -->
   <logger level="TRACE" name="io.vyne.query.GraphSearcher"/>

   <root level="INFO">
      <appender-ref ref="console"/>
   </root>

</configuration>
