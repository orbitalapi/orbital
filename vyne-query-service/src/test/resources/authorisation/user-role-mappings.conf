userRoleMappings {
   user1 {
       roles = ["Viewer", "QueryRunner",	"PlatformManager"]
   }

   userWhoseRolesTobeModified {
          roles = ["Viewer", "QueryRunner",	"PlatformManager"]
      }

   adminUser {
      roles = ["Admin"]
   }

   queryExecutor {
      roles = ["QueryRunner"]
   }

   viewer {
      roles = ["Viewer"]
   }

   platformManager {
      roles = ["PlatformManager"]
   }

   userWithoutAnyRoleSetup {
      roles = []
   }
}
