import { TocHostDirective } from './toc-host.directive';

describe('TocHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TocHostDirective();
    expect(directive).toBeTruthy();
  });
});
