server:
  port: 9024
spring:
  application:
    name: credit-pricing
vyne:
   schemaStore:
      name: discovery
   schema:
      name: credit-markup
      version: 0.1.0
logging:
  level:
      io.vyne: DEBUG
      io.osmosis: DEBUG
ribbon:
   restclient:
      enabled: true
   ServerListRefreshInterval: 1000
