authenticationTokens {
   MyService {
      tokenType = Header
      valuePrefix = Bearer
      paramName = Authorization
      value = ${foo} // Foo will get substituted from a fallback (normally the env)
   }
}
