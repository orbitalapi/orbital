server:
   port: 8080

spring:
   datasource:
      username: sa
      url: jdbc:h2:mem:testdb

keycloak:
   server:
      contextPath: /auth
      adminUser:
         username: vyneAdmin
         password: vyne
      realmImportFile: vyne-realm.json
      realmUsers:
         -
            username: user1
            password: test
         -
            username: user2
            password: test
