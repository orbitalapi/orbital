package com.orbitalhq.query.graph

/**
 * A store which wraps a GraphSearcher, caching and favouring
 * previous searches which have been successful before attempting
 * new searches
 *
 */
class GraphSearchStore {
}
