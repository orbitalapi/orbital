
## This directory contains setup for

* deploying a Grafana / Prometheus instance, reading logs locally from Orbital and Pipeline


### Grafana / Prometheus

To see the Orbital metrics data published scrapped by Prometheus on Grafana.

Run:

```shell
docker-compose up -d
``
